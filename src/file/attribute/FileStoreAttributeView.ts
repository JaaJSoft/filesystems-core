import {FileAttributeView} from "./FileAttributeView";

/**
 * An attribute view that is a read-only or updatable view of the attributes of
 * a {@link FileStore}.
 */
export interface FileStoreAttributeView extends FileAttributeView {

}
